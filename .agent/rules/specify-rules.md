# opus projects Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-27

## Active Technologies
- Dart ≥ 3.2 with sound null-safety + supabase_flutter (Auth + Storage), google_sign_in (Android native OAuth), flutter_secure_storage, image_cropper, riverpod_generator, go_router, freezed (002-auth-user-management)
- Drift/SQLite (local profile), Supabase PostgreSQL + Auth + Storage (remote) (002-auth-user-management)
- Dart ≥ 3.2 (sound null-safety) + Drift 2.x (SQLite), Riverpod 2.x, drift_flutter (003-local-db-offline-storage)
- SQLite via Drift (local), schema version 3 (003-local-db-offline-storage)

- Dart ≥ 3.2 with sound null-safety + Flutter SDK, Riverpod (riverpod_generator), GoRouter, (001-foundation-base-architecture)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

# Add commands for Dart ≥ 3.2 with sound null-safety

## Code Style

Dart ≥ 3.2 with sound null-safety: Follow standard conventions

## Recent Changes
- 003-local-db-offline-storage: Added Dart ≥ 3.2 (sound null-safety) + Drift 2.x (SQLite), Riverpod 2.x, drift_flutter
- 002-auth-user-management: Added Dart ≥ 3.2 with sound null-safety + supabase_flutter (Auth + Storage), google_sign_in (Android native OAuth), flutter_secure_storage, image_cropper, riverpod_generator, go_router, freezed

- 001-foundation-base-architecture: Added Dart ≥ 3.2 with sound null-safety + Flutter SDK, Riverpod (riverpod_generator), GoRouter,

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
